# JSON_validator_Rust
A CLI mini JSON validator that allows users to accept a text input and validate if it's proper JSON. The validator returns a message indicating if the JSON is valid or not and where the error is.
